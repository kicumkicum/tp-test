{
  "name": "torrent-proxy-server",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "10.x.x",
    "npm": "6.1.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf ./lib",
    "prebuild": "npm run clean",
    "build": "tsc",
    "watch": "npm run prebuild && tsc-watch --onSuccess \"npm run start\" --onFailure \"echo Beep! Compilation Failed\"",
    "deploy": "gcloud app deploy --project api-torrent-proxy",
    "prestart": "npm run build",
    "start": "node index.js"
  },
  "dependencies": {
    "bt-stream": "github:torrent-proxy/bt-stream#master",
    "express": "^4.17.1",
    "jsdom": "^16.0.1",
    "memory-chunk-store": "1.3.0",
    "node-fetch": "2.6.0",
    "torrent-stream": "1.2.0"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.11.0",
    "@types/node": "^8.5.2",
    "tsc-watch": "^1.0.15",
    "typescript": "2.9.1"
  }
}
